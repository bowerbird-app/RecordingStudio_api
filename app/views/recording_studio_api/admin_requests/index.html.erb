<div class="mx-auto flex w-full max-w-7xl flex-col gap-6 pb-6">
  <%= render FlatPack::PageNav::Component.new(
    entries: [
      {
        label: "Back",
        url: RecordingStudioApi.admin_dashboard_path(controller: self),
        icon: :arrow_left
      }
    ],
    anchor_url: page_nav_close_url
  ) %>

  <%= render FlatPack::PageTitle::Component.new(
    title: "Admin API requests",
    subtitle: "Filter and inspect admin API request volume.",
    variant: :h1
  ) %>

  <%= render partial: "content" %>
</div>
