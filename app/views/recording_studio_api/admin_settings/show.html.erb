<div class="mx-auto flex w-full max-w-6xl flex-col gap-6">
  <% recording_studio_page_nav(
    title: "Admin API settings",
    page_nav_back_url: RecordingStudioApi.admin_dashboard_path(controller: self),
    page_nav_back_label: "Back",
    page_nav_anchor_url: page_nav_close_url
  ) %>

  <%= render FlatPack::PageTitle::Component.new(
    title: "Admin API settings",
    subtitle: "Read-only view of the current API configuration loaded for this environment.",
    variant: :h1
  ) %>

  <%= render FlatPack::SectionTitle::Component.new(
    title: "Current configuration",
    subtitle: "These values reflect the active RecordingStudioApi configuration at runtime."
  ) %>

  <%= render FlatPack::Table::Component.new(data: @settings_rows) do |table| %>
    <% table.column(title: "Setting", html: ->(row) { row.fetch(:setting) }) %>
    <% table.column(title: "Value", html: ->(row) { row.fetch(:value) }) %>
  <% end %>
</div>
